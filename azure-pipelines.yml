trigger:
- main

pool:
  name: 'Default'

variables:
- group: esmos-secrets

steps:
- task: SSH@0
  displayName: 'Inject Secrets and Deploy to Staging'
  inputs:
    sshEndpoint: 'ssh-odoo17-staging'
    runOptions: 'commands'
    commands: |
      cd /home/G02T04
      [ -d "e08g02t04-esmos-project" ] || git clone https://github.com/Nigellzy/e08g02t04-esmos-project.git
      cd e08g02t04-esmos-project
      git pull
      echo "POSTGRES_PASSWORD=$(DB_PASSWORD)" > .env
      echo "MARIADB_PASSWORD=$(MOODLE_DB_PASSWORD)" >> .env
      echo "ODOO_ADMIN_PASSWORD=$(ODOO_ADMIN_PASSWORD)" >> .env
      chmod +x update_code.sh
      ./update_code.sh
    readyTimeout: '20000'